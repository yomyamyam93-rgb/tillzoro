# Till Zero 서비스 규칙 문서

버전 0.5 · 2026-09-03 · 상태: 초안 (개발 착수 전 확정용)

이 문서는 Till Zero가 어떻게 동작하는지를 정의한다. 디자인과 개발은 이 문서를 기준으로 진행하고, 규칙을 바꿀 때는 이 문서를 먼저 고친다.

0.4에서 바뀐 것: 정렬을 최근 대화순으로(R-4.8). 내 카드와 요청 칸(R-4.12).

0.3에서 바뀐 것: 첫 화면 구성과 맞틸 입구 위치(R-4.12, R-4.13). 프로필 규칙 추가(R-8.8, 사진 선택·기본 프로필). Gone 삭제. Zero는 흔적 없이 사라진다(5장 전면 수정). 재Bond 회차 표시(R-3.7) 삭제. 영구 Till 코드와 맞틸 추가(R-3.10, R-3.11). Zero 직후 대기 기간을 서버만 기억하는 것으로 정리(R-3.8).

---

## 1. 한 줄 정의

Till Zero는 30일 동안 서로 말이 없으면 서로에게서 사라지는 1:1 메신저다. 남은 사람이 진짜 곁에 있는 사람이다.

이 앱은 관계를 만들어주지 않는다. 연락을 대신해주지도 않는다. 하는 일은 딱 하나, 지키지 않은 관계를 화면에서 없애는 것이다. 그래서 남아 있는 목록이 곧 사용자의 진짜 인간관계가 된다.

**왜 30일인가.** 숫자의 기준은 "진짜 가까운 사이가 아무 이유 없이 말을 안 하게 되는 가장 긴 공백보다 조금 더 길 것"이다. 시험 기간, 여행, 훈련소, 가족 일 같은 공백은 관계가 식어서가 아니라 삶이 바빠서 생긴다. 7일이면 이런 공백이 전부 Zero로 처리되어 앱이 진짜 친구를 잘못 지우고, 그 순간 규칙의 권위가 무너진다. 30일 동안 한 마디도 없었다면 변명이 없다. Zero가 정직해야 남은 목록이 의미를 가진다. 짧은 주기는 대화를 관계가 아니라 숫자를 위한 것으로 만든다(스냅챗 스트릭이 겪은 문제, 13장 참고). 주기가 짧으면 유예가 필요해지고, 유예를 넣지 않는다는 원칙(R-5.7)과 충돌한다. 30일은 유예 없음과 함께 갈 수 있는 가장 짧은 주기다.

30일은 첫 버전의 값이다. 시험 사용에서 사라진 관계를 본 사용자가 "맞아"라고 하면 유지하고, Zero가 너무 드물어 아무 일도 일어나지 않으면 14일로 줄이는 것을 검토한다. 줄이는 것은 "더 진지해졌다"로 읽히지만 늘리는 것은 "봐줬다"로 읽히므로, 처음 값은 긴 쪽에서 시작한다.

**사용자 나이에 따라 목록의 크기가 다르다.** 10대는 수십 명이 살아 있을 수 있고, 30~40대는 두세 명일 수 있다. 같은 규칙이 10대에게는 "이만큼 지키고 있다"는 훈장이 되고, 어른에게는 "정말 곁에 있는 사람"을 비추는 거울이 된다. 둘 다 이 앱이 의도한 경험이다. 화면은 양쪽 모두에서 성립해야 한다(R-4.10).

## 2. 용어

용어는 영어 단어 하나로 통일하고, 화면·코드·문서에서 같은 단어를 쓴다. 한국어 화면 문구에서도 이 단어를 그대로 쓴다(예: "Count 7", "Keep 18"). 단, 맞틸은 앱 밖에서 사람들이 쓰는 말이지 앱이 강조하는 용어가 아니다. 화면 문구, 버튼, 라벨에 "맞틸"을 쓰지 않는다. 앱 안에서는 그냥 "초대", "요청"이다.

**Bond**: 두 사용자가 서로 수락해서 맺어진 1:1 관계. 이 앱의 모든 것은 Bond 단위로 움직인다. Bond 하나에 대화방 하나, Count 하나가 붙는다.

**Count**: Bond마다 붙는 남은 일수. 30에서 시작해 매일 1씩 줄고, Reset되면 30으로 돌아간다.

**Reset**: Count가 30으로 돌아가는 일. 조건은 4장에서 정한다.

**Zero**: Count가 0이 되어 Bond가 끝나는 일. 양쪽 모두에게서 동시에 일어난다. 앱 이름 Till Zero의 그 Zero다.

**Keep**: 한 Bond 안에서 Reset이 일어난 횟수. "함께 지킨 횟수". Bond가 쌓아온 것을 나타내는 핵심 숫자.

**Till**: 살아 있는 Bond들이 모여 있는 첫 화면이자 이 앱의 유일한 목록. 계산대 돈통(till)처럼 "내가 가진 것"이 들어 있는 곳. "너 내 Till에 있어?"가 이 앱의 일상 말이다.

**Days**: 한 Bond가 끊기지 않고 이어진 일수. Bond를 맺은 날부터 센다.

**맞틸**: 서로 Till에 넣는 것. 맞팔처럼 쓰는 말이며, Bond를 맺는 행위의 일상 표현이다. 앱 밖(인스타그램, 카톡 프로필 등)에서 "맞틸 구해요"로 퍼진다.

이 앱에는 사라진 것을 보관하는 목록이 없다. Zero가 된 Bond는 흔적 없이 사라진다(5장).

## 3. Bond 맺기

**R-3.1 초대는 링크 또는 코드로만 한다.** 앱은 사용자의 휴대폰 주소록을 읽지 않는다. 친구 추천, 아는 사람 찾기, 연락처 동기화 기능은 만들지 않는다. 앱을 쓰지 않는 사람의 정보를 앱이 갖지 않기 위해서다.

**R-3.2 초대는 양쪽이 모두 수락해야 성립한다.** A가 초대 링크를 보내고 B가 열어서 수락하면 Bond가 맺어진다. 한쪽만으로는 아무 일도 일어나지 않는다.

**R-3.3 초대 링크는 7일 뒤 만료된다.** 만료된 링크를 열면 "이 초대는 지났어요"만 보여주고 아무 정보도 노출하지 않는다.

**R-3.4 Bond가 맺어지는 순간 Count는 30에서 시작한다.** 대화방도 이때 생긴다. Bond 이전에는 대화가 불가능하다.

**R-3.5 맺을 수 있는 Bond 수에 상한은 없다.** 몇 명을 유지하느냐는 사용자의 몫이다. 앱은 숫자를 제한하지 않는다.

**R-3.6 Zero가 된 상대와 다시 Bond를 맺을 수 있다. 단, 쌓은 것은 돌아오지 않는다.** 새 Bond는 Keep 0, Days 1, 빈 대화방에서 시작한다. 이전 Bond의 대화, Keep, Days는 Zero 시점에 이미 사라졌으므로 돌아올 것이 없다. 잃는 것은 사람이 아니라 둘이 쌓아온 시간이고, 그것이 Zero의 값이다. 다시 맺는 것을 막지 않는 이유는, 관계는 끊겼다가 다시 이어질 수 있고 앱이 그걸 막는 건 앱의 역할이 아니기 때문이다. 다시 맺은 Bond는 처음 맺은 Bond와 구별되지 않는다. 회차 표시는 없다.

**R-3.7 (삭제, 0.4)** 재Bond 회차 표시를 두지 않는다. 기록을 남기지 않는 앱이 회차만 남기는 것은 모순이다.

**R-3.8 Zero 직후에는 바로 다시 맺을 수 없다.** Zero가 된 시점부터 7일 동안은 같은 상대와 Bond를 맺을 수 없다. 끊겼다는 사실을 실감할 시간이고, "0 되면 다시 맺으면 되지"로 Zero를 가볍게 만드는 걸 막기 위해서다. 이 대기는 서버가 두 계정의 쌍과 Zero 시각만 기억해서 처리하고, 7일이 지나면 그 기록도 지운다. 사용자 화면에는 초대 시도 때 "며칠 뒤에 다시 맺을 수 있어요"만 보인다. 7일이라는 값은 시험 사용 뒤 조정한다.

**R-3.9 초대를 거절하면 초대한 쪽에는 아무것도 알리지 않는다.** 초대한 쪽 화면에는 링크가 만료될 때까지 "기다리는 중"으로만 보인다.

**R-3.10 초대 수단은 두 가지다.** 1회용 링크와 영구 Till 코드. 1회용 링크는 아는 사람에게 직접 보낼 때 쓰고 7일 뒤 만료된다(R-3.3). 영구 Till 코드는 인스타그램 프로필, 카톡 프로필처럼 앱 밖에 걸어두는 용도로, 사용자가 켜고 끌 수 있으며 끄면 즉시 무효가 된다. 코드로 들어온 요청도 R-3.2에 따라 내가 수락해야 Bond가 된다. 코드는 언제든 새로 발급할 수 있고, 새로 발급하면 이전 코드는 무효가 된다.

**R-3.11 맞틸은 넓게 열고, 남는 것은 시간이 정한다.** 앱은 맞틸의 상대가 아는 사람인지 확인하지 않고, 맞틸 수를 제한하지 않는다. 모르는 사람과 백 명을 맞틸해도 30일 안에 서로 한 마디를 주고받지 않은 Bond는 Zero가 되어 흔적 없이 사라지므로, 남는 것은 실제로 말을 주고받은 사람뿐이다. 이것이 맞틸이 "적을수록 진짜"와 충돌하지 않는 이유다. 다만 영구 코드로 들어오는 요청이 많아질 경우를 대비해, 코드로 온 요청은 링크로 온 요청과 구분해 표시하고 한 번에 여러 건을 거절할 수 있게 한다.

## 4. Count와 Reset

**R-4.1 Count는 마지막 Reset 시각을 기준으로 계산한다.** 저장하는 값은 "남은 일수"가 아니라 "마지막 Reset 시각(lastResetAt)"이다. 남은 일수는 볼 때마다 계산한다.

```
remaining = 30 - floor((now - lastResetAt) / 24h)
if remaining <= 0 → Zero
```

기기 시간이 아니라 서버 시간을 쓴다. 사용자가 폰 시계를 바꿔도 Count는 영향받지 않는다.

**R-4.2 Reset 조건: 마지막 Reset 이후 양쪽이 각각 한 번 이상 메시지를 보냈을 때.** 순서는 상관없다. A가 먼저 보내든 B가 먼저 보내든, 둘 다 한 번씩 보낸 시점에 Count가 30으로 돌아간다. Reset 시각은 두 번째 사람의 메시지가 서버에 도착한 시각이다.

**R-4.3 한쪽만 보낸 메시지는 Reset이 아니다.** A가 1일에 보내고 B가 답하지 않으면 Count는 계속 줄어든다. B가 29일에 답하면 그때 Reset된다. B가 끝까지 답하지 않으면 0이 되어 사라진다. 답하지 않는 것도 답이다.

**R-4.4 Reset 직후 보낸 메시지는 다음 Reset의 첫 번째 메시지가 된다.** Reset되자마자 A가 또 보내면, B가 답하는 순간 다시 Reset된다. 자주 대화하는 사이는 Count가 거의 항상 30에 가깝게 유지된다.

**R-4.5 메시지로 인정되는 것.** 텍스트, 사진, 이모지 단독 전송 모두 메시지다. 읽기, 입력 중 표시, 반응(리액션)은 메시지가 아니다. 사용자가 손가락 하나로 Reset을 만들 수 없게 하기 위해서다. 빈 메시지는 보낼 수 없다.

**R-4.6 (삭제, 0.3)** Reset 구분선을 대화방에 넣지 않는다. 대화방은 대화만 보여주고, 남은 일수는 Count로, 쌓은 것은 Keep과 Days(R-7.7)로 충분하다. 번호는 참조 유지를 위해 남긴다.

**R-4.7 Count 표시 상태.** 남은 일수에 따라 화면 표현이 바뀐다. 색 자체는 디자인 가이드에서 정하고 여기서는 단계만 정한다.

| 남은 일수 | 상태 | 표현 원칙 |
|---|---|---|
| 30~15 | 편안함 | 숫자만 조용히 |
| 14~8 | — | 변화 없음 (옅어짐은 보류, 디자인 가이드 12장) |
| 7~4 | — | 변화 없음 |
| 3~1 | 위급 | 숫자와 라벨이 붉어짐 (자리는 정렬 그대로) |
| 0 | Zero | Till에서 사라짐 |

**R-4.8 Till 정렬은 Count 높은 순.** 30인 사람이 맨 위, 아래로 갈수록 남은 날이 적고, 곧 Zero가 될 사람이 맨 끝에 온다. 위에서 아래로 읽으면 관계의 온도 순서가 된다. 같은 Count면 마지막 대화가 최근인 순, 그다음 이름순. (0.5에서 변경: 이전에는 Count 오름차순이었다. 곧 사라질 사람을 맨 위에 두는 것은 목록을 할 일 목록으로 만들고 사용자를 재촉하는 구조라 브랜드 원칙 "재촉하지 않음"에 어긋난다. 경고는 6장의 알림이 맡는다.) 최근 대화순·이름순 외의 정렬 옵션은 만들지 않는다.

**R-4.9 Till이 비어 있을 때.** Bond가 하나도 없으면 "아직 아무도 없어요" 한 줄과 설정으로 가는 안내만 보인다. 추천 친구, 둘러보기, 예시 화면은 없다. 처음 한 번만 "설정에서 초대 링크를 만들 수 있어요"를 보여주고, 그 뒤로는 반복하지 않는다.

**R-4.10 적을수록 크게.** 살아 있는 Bond가 적을수록 이름과 숫자를 크게 보여주고, 많을수록 촘촘하게 보여준다. 세 명인 화면이 텅 비어 보이지 않아야 하고, 서른 명인 화면이 지저분하지 않아야 한다. 구체적인 단계는 디자인 가이드에서 정하되, 원칙은 "세 명도 꽉 찬 화면"이다.

**R-4.11 Till 상단에 살아 있는 Bond 수를 표시한다.** "지금 곁에 있는 사람 4명" 형태. 이 숫자는 통계가 아니라 이 앱이 사용자에게 주는 유일한 요약이다. 순위, 평균, 다른 사용자와의 비교는 하지 않는다.

**R-4.12 첫 화면은 세 가지로 끝난다.** 살아 있는 Bond 수, 목록, 그리고 목록 맨 위의 내 카드. 내 카드는 Count 자리에 ∞, Keep 자리에 태어난 날부터 센 30일 횟수가 보이고, 누르면 설정이다. 나는 이 앱에서 유일하게 Zero가 되지 않는 관계이므로 ∞가 맞다. 별도의 설정 버튼은 두지 않는다. 맞틸 입구(초대 링크 만들기, 내 Till 코드)는 설정 안에 둔다. 첫 화면에서 맞틸을 버튼으로 드러내면 앱이 "친구를 초대하세요"를 상시 광고하는 것이 되고, 이는 브랜드가 하지 않는 일이다. 받은 요청은 요청이 있을 때만 목록 맨 아래에 + 칸으로 보이고, 없으면 아무것도 보이지 않는다. 누르면 요청한 사람과 경로(링크/코드)가 보이고 수락 또는 거절한다.

**R-4.13 앱의 화면은 네 개다.** Till, 대화방, 설정, 그리고 설정 안의 맞틸. 이 밖의 화면은 만들지 않는다. 빈 Till은 Till의 한 상태이고, 차단 목록은 설정의 한 항목이다.

## 5. Zero

**R-5.1 Count가 0이 되면 Bond는 양쪽에서 동시에 사라진다.** 한쪽만 남는 경우는 없다. 벌이 아니라 둘이 함께 지는 것이다.

**R-5.2 Zero는 흔적을 남기지 않는다.** Till에서 그 사람이 없어지고, 대화방과 대화 기록, Keep, Days가 함께 사라진다. 사라진 Bond를 보관하는 목록은 없다. 다음에 앱을 열면 한 명이 줄어 있을 뿐이고, 앱은 아무 설명도 하지 않는다. 지금 Till에 있는 사람이 전부다.

**R-5.3 Zero는 알림을 보내지 않는다.** 푸시도, 배지도, 앱 안의 안내 화면도 없다. 사용자가 앱을 열었을 때 발견한다. Zero를 알림으로 만들면 이 앱은 협박이 된다.

**R-5.4 Zero는 상대에게 "누가 나를 놓쳤는지"의 형태로 보이지 않는다.** 양쪽 모두 같은 화면을 본다. "OO이 답하지 않아서 사라졌어요" 같은 문구는 어느 쪽에도 쓰지 않는다. 둘 사이의 일은 둘만 안다.

**R-5.5 Zero에 유예는 없다.** 0이 되기 직전 마지막 알림(6장)까지가 앱이 해주는 전부다. 휴가 모드, 일시정지, 보호 기능, 복구는 만들지 않는다. 규칙이 흔들리면 남은 목록의 의미가 사라진다.

**R-5.6 서버의 데이터 삭제.** Zero 시점에 대화 기록은 서버에서도 삭제한다. 남기는 것은 R-3.8의 대기 처리를 위한 계정 쌍과 Zero 시각(7일 뒤 삭제), 그리고 R-8.6의 신고 대응용 로그(90일, 사용자 비노출)뿐이다.

**R-5.7 (삭제, 0.4)** Gone 목록과 그에 딸린 규칙(흑백 표시, 읽기 전용 기록, 본인만 열람)은 모두 삭제한다. 사라진 것을 보관하는 화면은 뺄셈이 아니라 덧셈이고, 후회를 쌓는 화면이며, 캡처되어 돌아다닐 때 괴롭힘의 증거가 되는 화면이다.

## 6. 알림

**R-6.1 이 앱의 알림은 세 종류뿐이다.** 곁에 있는 사람의 메시지, 초대 수락, 그리고 Count 경고. 그 외의 알림(마케팅, 추천, 기능 안내)은 보내지 않는다. 이 앱의 알림은 전부 중요해야 한다.

**R-6.2 Count 경고는 Bond당 두 번만 보낸다.** 남은 일수가 7이 되는 시점에 한 번, 1이 되는 시점에 한 번. 문구는 상대 이름과 남은 일수만 담는다. 예: "지민 · 7일 남았어요". 위협하는 말투를 쓰지 않는다.

**R-6.3 경고 알림은 Reset되면 초기화된다.** Reset 후 다시 7일이 되면 다시 한 번 보낸다.

**R-6.4 한쪽이 보내고 답을 기다리는 상태는 목록에서 보인다.** 내가 보냈고 상대가 아직 안 보낸 Bond에는 "답 기다리는 중" 표시가 붙는다. 상대 쪽에는 보통 대화처럼 안 읽은 메시지로만 보인다.

## 7. 대화

**R-7.1 대화는 1:1만 지원한다.** 그룹 대화는 첫 버전에서 만들지 않는다. Bond는 두 사람 사이의 것이다.

**R-7.2 대화 기록은 남는다.** 사라지는 메시지, 자동 삭제는 없다. 기록이 쌓이는 것 자체가 이 앱의 자산이다. 사라질 때 잃는 것이 클수록 사람들은 지킨다.

**R-7.3 보낼 수 있는 것.** 텍스트, 사진, 이모지. 영상, 파일, 음성, 위치, 링크 미리보기는 첫 버전에서 제외한다. 필요해지면 그때 추가한다.

**R-7.4 읽음 표시는 하지 않는다.** 안 읽음 숫자도 없다. 이 앱에서 압박은 Count 하나면 충분하다. 읽고 답하지 않은 것을 드러내는 장치는 관계를 망가뜨린다.

**R-7.5 입력 중 표시는 하지 않는다.** 같은 이유다.

**R-7.6 메시지 삭제는 내 화면에서만 가능하다.** 상대 화면의 메시지를 지우는 "전송 취소"는 만들지 않는다. Reset 조건을 되돌리는 데 악용될 수 있기 때문이다. 이미 보낸 메시지는 보낸 것으로 친다.

**R-7.7 Bond마다 기록을 보여준다.** 대화방 상단(또는 상대 정보 화면)에 맺은 날, Keep, Days를 표시한다. "2024년 3월부터 · Keep 18 · Days 540". 이 숫자들이 두 사람이 쌓아온 것이고, Zero가 될 때 통째로 잃는 것이다. 이 기록은 두 사람만 본다.

## 8. 계정, 나이, 안전

**R-8.1 가입은 휴대폰 번호 인증으로 한다.** 번호는 계정 식별과 중복 가입 방지에만 쓰고, 다른 사용자에게 절대 노출하지 않는다. 번호로 사람을 찾는 기능은 없다.

**R-8.2 만 14세 미만은 가입할 수 없다.** 가입 시 생년월일을 받고 기준 미달이면 가입을 막는다. 청소년이 주 사용층이 될 수 있으므로, 이후 법정대리인 동의 흐름을 붙일 수 있게 구조를 열어둔다.

**R-8.3 계정을 지우면 모든 Bond가 Zero와 똑같이 사라진다.** 상대의 Till에서 그냥 없어진다. 이유도, 안내도 없다. Zero와 구별되지 않는다.

**R-8.4 계정을 지우면 기록은 복구되지 않는다.** 같은 번호로 다시 가입해도 새 계정이다. 이전의 Bond, Keep, Days, 대화는 돌아오지 않는다. 계정 삭제 시 번호 해시를 포함한 모든 개인정보를 지운다.

**R-8.5 차단과 신고는 모든 대화방에 있다.** 차단하면 그 Bond는 즉시 Zero와 똑같이 사라진다. 상대에게는 Count 0으로 사라진 것과 구별되지 않는다. 차단당했다는 사실은 알 수 없다. 차단 목록은 설정 안에만 있고, 차단을 풀기 전까지 그 상대와는 다시 맺을 수 없다.

**R-8.6 앱은 Zero를 이용한 괴롭힘에 대비한다.** 여러 사람이 한 명에게 동시에 답을 끊는 식의 사용은 규칙상 막을 수 없지만, 신고가 들어오면 확인할 수 있게 Zero 로그를 90일 보관한다. 이 로그는 사용자에게 노출되지 않는다.

**R-8.8 프로필은 닉네임과 사진뿐이다.** 닉네임은 필수, 사진은 선택이다. 사진을 올리지 않은 사용자에게는 앱이 정한 기본 프로필이 보인다. 카톡처럼 공개하고 싶지 않은 사람을 위한 자리다. 기본 프로필은 한 가지로 통일하지 않고 몇 가지 중 자동 배정해, 사진 없는 사람이 여럿일 때도 서로 구별되게 한다. 상태 메시지, 소개글, 사진을 눌렀을 때 열리는 프로필 화면은 없다. 사람을 알아보는 데 필요한 최소는 이름과 얼굴이고, 그 이상은 두지 않는다.

**R-8.7 저장하는 데이터는 최소로 한다.** 번호 해시, 닉네임, Bond 관계, 메시지, Reset 시각. 이 다섯 가지 외에는 저장하지 않는다. 위치, 주소록, 기기 정보(필수 기술 정보 제외)는 수집하지 않는다.

## 9. 문구 원칙

**R-9.1 Zero를 협박처럼 쓰지 않는다.** "친구를 잃게 됩니다" 같은 표현은 어디에도 쓰지 않는다. 남는 것을 자랑스럽게 말한다. "지금 곁에 있는 사람 4명" 같은 식으로.

**R-9.2 숫자는 항상 이름 옆에 있다.** 숫자 혼자 떠다니지 않는다. 이 숫자는 통계가 아니라 그 사람과 나 사이의 것이다.

**R-9.3 존댓말도 반말도 아닌, 짧은 말.** 화면 문구는 최대한 짧게. "7일 남았어요", "30으로 돌아왔어요", "아직 대화가 없어요". 설명이 길어지면 규칙이 복잡해 보인다. Zero에는 문구가 없다. 사라진 것에 대해 앱은 말하지 않는다.

**R-9.4 앱 스토어 설명은 남는 쪽을 말한다.** "진짜 곁에 있는 사람만 남는 메신저"가 기본 문장이다. "사라진다"는 두 번째 문장에서 규칙으로만 설명한다.

## 10. 첫 버전에서 만들지 않는 것

이 목록은 기능 요청이 들어올 때 다시 읽는 용도다. 넣지 않는 이유가 있다.

그룹 대화는 Bond가 둘 사이의 것이기 때문에 넣지 않는다. 주소록 연동은 동의하지 않은 사람의 정보를 갖게 되므로 넣지 않는다. 읽음 표시와 입력 중 표시는 Count 외의 압박을 만들므로 넣지 않는다. 휴가 모드와 일시정지는 규칙을 흔들므로 넣지 않는다. Bond 수 상한은 사용자의 몫이므로 넣지 않는다. 사라진 Bond의 목록이나 기록 보관은 뺄셈에 어긋나고 괴롭힘의 증거가 되므로 넣지 않는다. 프로필 사진 외의 프로필(상태 메시지, 소개글)은 이 앱이 SNS가 아니므로 넣지 않는다. Zero 뒤 기록을 이어받는 "복구"는 Zero의 값을 없애므로 넣지 않는다.

## 11. 예외 상황 정리

**둘이 동시에 첫 메시지를 보냈다.** 서버 도착 순서로 첫 번째와 두 번째를 정하고, 두 번째 도착 시각에 Reset한다. 사용자는 차이를 느끼지 못한다.

**메시지를 보냈는데 네트워크 오류로 서버에 안 갔다.** 서버에 도착하지 않은 메시지는 Reset 조건에 포함되지 않는다. 앱은 전송 실패를 명확히 표시한다.

**Count가 0이 되는 순간 메시지가 도착했다.** 서버 시각 기준으로 Zero 처리가 먼저면 메시지는 거부되고, 보낸 쪽 화면에서는 그 대화방이 사라진다. 메시지가 먼저면 정상 처리된다. 경계는 서버가 정한다.

**한쪽이 앱을 오래 안 열었다.** Count는 앱을 열지 않아도 줄어든다. Zero도 앱을 열지 않아도 일어난다. 다시 열었을 때 Till에서 그 사람이 없어져 있는 것으로 발견한다.

**상대가 계정을 지웠다.** R-8.3에 따라 Zero와 똑같이 Till에서 사라진다. 상대가 다시 가입하면 새 계정이므로 새로 맞틸해야 하고, 기록은 이어지지 않는다.

**차단한 상대가 다시 초대 링크나 코드를 보냈다.** 초대 자체가 생성되지 않는다. 차단한 쪽에는 아무것도 보이지 않는다.

**Zero 뒤 7일 안에 초대를 시도했다.** R-3.8에 따라 초대가 생성되지 않고, 보내는 쪽에 "며칠 뒤에 다시 맺을 수 있어요"만 표시한다.

**시간대가 다른 두 사람.** Count는 서버 UTC 기준으로 계산하고, 표시는 각자 기기 시간대로 한다. "7일 남음"의 정확한 시각은 사람마다 몇 시간 다를 수 있지만 일수 단위 표시라 문제되지 않는다.

## 12. 아직 정하지 않은 것

닉네임을 실명으로 강제할지 여부. 기본 프로필의 종류와 수. 사진 메시지의 용량과 보관 기간. Days를 Till 목록에도 보여줄지, 대화방 안에서만 보여줄지. 만 14세 이상 청소년의 법정대리인 동의를 어느 시점에 넣을지. R-3.8의 재Bond 대기 기간(7일)이 적절한지. 영구 Till 코드로 오는 요청에 하루 상한을 둘지. 이 일곱 가지는 디자인 단계와 법률 검토에서 같이 정한다.

## 13. 유사 서비스와의 차이

Till Zero를 이루는 조각은 각각 어딘가에 있지만, 합쳐진 형태는 없다. 개발과 마케팅에서 "무엇과 다른가"를 물을 때 이 장을 참조한다.

**스냅챗 스트릭**은 24시간 안에 서로 보내야 연속 기록이 유지된다. "서로 한 번씩"이라는 Reset 조건은 같다. 다른 점은 스냅챗은 잃는 것이 숫자이고 Till Zero는 잃는 것이 사람이라는 것, 그리고 주기가 24시간이라 사람들이 숫자를 위해 의미 없는 스냅을 보내다 지쳤고 결국 일시정지·복구 기능이 생겼다는 것이다. Till Zero의 30일과 유예 없음은 이 경험에서 나온 결정이다.

**범블 BFF**는 매칭 후 하루 안에 메시지가 없으면 연결이 사라진다. 사라지는 구조는 같지만 새로 만난 사람용이고, 기존 관계에는 쓰이지 않는다.

**realfriends**는 친구를 25명으로 제한하는 메신저다. "소수만 남긴다"는 방향은 같지만 숫자로 자르고, Till Zero는 시간으로 거른다. 몇 명이 남을지는 사용자와 시간이 정한다.

**Tend, Catchup, Garden 같은 연락 리마인더**는 누구에게 연락한 지 얼마나 됐는지 보여주고 알림을 준다. 혼자 쓰는 관리 도구이고 상대는 앱을 쓰지 않는다. Till Zero는 둘 다 앱 안에 있고, 둘 다 같은 숫자를 보고, 둘 다 같이 잃는다. 이 대칭이 가장 큰 차이다.

한 문장으로 하면, Till Zero는 "양쪽이 함께 지키고 함께 잃는 메신저"이고 이 자리는 비어 있다.

---

이 문서의 규칙 번호(R-x.x)는 디자인 화면과 개발 이슈에서 그대로 참조한다. 규칙을 바꿀 때는 번호를 유지하고 내용만 고친 뒤 버전을 올린다.
