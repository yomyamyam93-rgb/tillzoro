# Till Zero 디자인 가이드

버전 0.1 · 2026-09-03 · tokens.css와 짝. 규칙 문서 v0.4, 브랜딩 기획 v0.2 기준.

이 문서는 "왜 그 값인가"를 적는다. 값 자체는 tokens.css가 원본이고, index.html이 그 값을 그대로 쓴다. 여기 없는 결정은 아직 안 한 것이다(12장).

## 1. 정해진 것 (2026-09-03)

카드 높이는 전부 같고 얇다. 사진 76px. 비교판 01에서 "B의 12 D-DAY 카드처럼 얇게"를 골랐고, 그 값이 76이다. 높이를 D-DAY에 따라 바꾸지 않는다. 규칙이 하나 줄고, 서른 명일 때 스크롤이 짧아진다.

대화방 헤더는 150px(상태바 54 별도, 합쳐서 204 영역). 사진이 상태바까지 채우고 아래 모서리 24px. 헤더 안 정보는 왼쪽 아래에 D-DAY 숫자(44px)가 먼저, 그 오른쪽에 이름과 KEEP · DAYS. 목록 줄과 같은 순서(숫자 → 사람)라 눈이 헷갈리지 않는다.

말풍선은 꼬리 쪽만 각지다(비교판 05-B). 모서리 16, 꼬리 4. 내 말은 잉크색 바탕에 종이색 글자, 상대는 연회색. 색은 없다.

기본 테마는 밝게. 어둡게는 설정에서 고른다. 시스템 따라가기는 세 번째 선택지.

글꼴은 두 개. 숫자(TILL, D-DAY, KEEP 값, 날짜 숫자, 초대 코드)는 Orbitron. 그 외 전부 Pretendard. 세 번째 글꼴은 없다.

## 2. 색

밝은 바탕 #FAFAF9, 카드 #FFFFFF, 잉크 #131416 / #5C5F66 / #9A9DA4 세 단계, 선 #E3E3E0. 순백과 순흑을 피하고 아주 약하게 따뜻한 회색 쪽으로 기울였다. 사진이 주인공이라 바탕이 사진과 싸우면 안 된다.

빨강 #E5232E는 이 앱의 유일한 색이고, D-DAY 3 이하의 숫자와 그 라벨에만 쓴다. 카드 테두리, 버튼, 말풍선에는 쓰지 않는다. 앱 아이콘의 빨강과 같은 계열이며, 앱 안에서 이 색을 보는 건 "보고 싶지 않은 순간"이다.

어두운 테마는 밝은 테마를 뒤집은 게 아니라 따로 골랐다. 바탕 #0C0C0E, 카드 #151518, 잉크 #F2F2EF. 빨강은 어두운 바탕에서 더 밝은 #FF2D3A.

## 3. 글자 크기

TILL 헤더 84, 목록 D-DAY 30, KEEP 값 16, 대화방 D-DAY 44. 라벨(D-DAY, KEEP) 9에 자간 .12em. 이름 15, 본문 14, 보조 13, 캡션 12. 숫자는 이름보다 두 배 크다. 이 앱에서 매번 새로운 정보는 숫자이므로 눈이 거기로 먼저 간다.

## 4. 간격과 모서리

화면 좌우 여백 20. 목록 카드 사이 14. 왼쪽 숫자 열 72. 모서리는 카드 14, 말풍선 16(꼬리 4), 입력 필드 14, 목록 박스 16, 시트 22, 헤더 하단 24, 버튼은 알약(높이 50, 반지름 25). 모서리가 커질수록 "떠 있는 것"이다. 카드보다 시트가, 시트보다 버튼이 더 둥글다.

## 5. 컨트롤

주 버튼은 잉크색 알약, 높이 50, 글자 15 세미볼드. 보조 버튼은 같은 모양에 카드색 바탕과 선. 아이콘 버튼은 34 원형, 아이콘 16, 선 2px 둥근 끝. 입력 필드 높이 52. 토글 46×28. 한 화면에 주 버튼은 하나.

## 6. 움직임

화면 전환은 오른쪽에서 밀려 들어오기 0.3초, 아래 화면은 14% 밀림. 그 외 움직임은 없다. 숫자는 세지 않고, 카드는 흔들리지 않고, 사라짐은 애니메이션 없이 그냥 없다. 로딩 화면의 0도 깜빡이지 않는다.

## 7. 목록 줄 해부

맨 위는 내 카드다. D-DAY 자리에 ∞(글꼴이 아니라 직접 그린 기호, 획 굵기는 숫자와 맞춤), KEEP 자리에 태어난 날부터 센 30일 횟수. 누르면 설정. 그 아래는 최근 대화순.

왼쪽 열(72): D-DAY 숫자 30 → 라벨 D-DAY → KEEP 값 16 → 라벨 KEEP, 세로로. 오른쪽 카드: 사진 76 위에 이름(왼쪽 아래, 흰색, 그림자), 아래 줄에 안 읽음 점 · 마지막 말 · 답 기다리는 중 · 날짜. 누르면 대화방. 카드 안에 버튼은 없다.

## 8. 대화방 해부

사진 헤더 150+54. 왼쪽 위 뒤로가기, 오른쪽 위 영상통화. 왼쪽 아래 D-DAY 44 → 이름 20 → KEEP · DAYS 10. 본문은 날짜 구분 한 줄(작은 알약)과 말풍선만. 입력줄은 + · 텍스트 · 보내기.

## 9. 문구

규칙 문서 9장을 따른다. 이 가이드에서 추가로 정한 것은 라벨의 영어 표기다. D-DAY, KEEP, DAYS, TILL은 대문자 영어로 쓰고 한국어로 번역하지 않는다. 화면의 그 외 모든 문구는 한국어.

## 10. 사진 다루기

목록 카드와 대화방 헤더의 사진은 가로로 자른다(카드 76, 헤더 150). 얼굴이 위쪽에 있는 사진이 많으므로 세로 기준점을 35% 지점에 둔다. 사진 위 글자는 항상 아래에서 위로 가는 검은 그라데이션 위에 놓는다. 프로필 사진 없는 사람의 카드는 미결(12장).

## 11. 하지 않는 것

테두리(1px 선)를 컨테이너에 쓰지 않는다. 카드·목록·입력창은 바탕(연회색 #F2F2F4)과 흰색의 대비로만 구분한다. 그림자는 떠 있는 것(설정 플로팅 버튼, 입력 바, 시트)에만 쓴다. 그라데이션을 사진 위 글자 보호 외에 쓰지 않는다. 아이콘에 색을 입히지 않는다. 배지에 숫자를 넣지 않는다(안 읽음은 점 하나). 이모지를 UI에 쓰지 않는다. 세 번째 글꼴을 들이지 않는다.

## 12. 아직 정하지 않은 것

기본 프로필(사진 없는 사람) 생김새 — 이미지 별도 제공 예정. 옅어짐 — 흐림은 보류, 흑백은 제외. 지금은 흐림 없이 숫자 색만으로 급함을 표시(--fade-* 전부 1). 대안으로 검토할 것: 숫자만 옅어짐, 카드 아래 얇은 남은 날 선, 사진 채도만 살짝 빠짐. 앱 아이콘 최종 형태. 세 명 이하일 때 카드를 키울지. 안 읽음 점 크기(지금 7px).
